# IRH Computational Environment
# Intrinsic Resonance Holography v26.0 - Scientific Computing Stack

name: irh-compute
channels:
  - conda-forge
  - defaults
dependencies:
  # Core Python
  - python=3.11
  
  # Numerical Computing
  - numpy>=1.24
  - scipy>=1.11
  - mpmath>=1.3                # Arbitrary precision arithmetic for α derivation
  - numba>=0.57                # JIT compilation for performance
  
  # Symbolic Mathematics
  - sympy>=1.12                # Symbolic computation for equation derivation
  
  # Visualization
  - matplotlib>=3.7
  - seaborn>=0.12
  - plotly>=5.15
  
  # Data Handling
  - pandas>=2.0
  - h5py>=3.9                  # HDF5 for large datasets
  
  # Graph Theory (Cymatic Resonance Network)
  - networkx>=3.1
  
  # Jupyter Environment
  - jupyter>=1.0
  - jupyterlab>=4.0
  - nbconvert>=7.0
  - nbformat>=5.9
  - ipywidgets>=8.0
  
  # Testing
  - pytest>=7.4
  - pytest-notebook>=0.8
  
  # Performance Monitoring
  - memory_profiler>=0.61
  - tqdm>=4.65
  
  # Pip-only packages
  - pip:
    # Quantum Computing (Braid Group Calculations)
    - qutip>=5.0.0
    
    # Spherical Harmonics (Hopf Fibration)
    - spherical-functions>=2022.4.5
    
    # Lie Algebra Calculations (Gauge Symmetry)
    - lie-learn>=0.0.1.post1
    
    # Topology Computations
    - giotto-tda>=0.6.0
    
    # Additional Math
    - primefac>=2.0.12          # Prime factorization utilities
    - galois>=0.3.6             # Finite field arithmetic
